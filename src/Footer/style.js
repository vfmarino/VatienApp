import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
        container: {
                height: 40,
                width: "100%",
                position: "absolute",
                bottom: 0,
                backgroundColor: "#178dd5",
                alignItems: "center",
                justifyContent: "center",
        },
});

export default styles;
